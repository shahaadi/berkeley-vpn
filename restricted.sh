# berkeley-vpn, Restricted VPN helper (optional add-on).
#
# Downloaded by `berkeley-vpn restricted` once you confirm you need it (that
# command shows who the Restricted VPN is for and how to get access). Its presence
# enables the restricted tunnel; `berkeley-vpn uninstall` removes it.
#
# connect.sh sources this file purely for the gateway host below, the one piece
# the core tool deliberately doesn't ship.
RVPN_GATEWAY="restricted.vpn.berkeley.edu"
